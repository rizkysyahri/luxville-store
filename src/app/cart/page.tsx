import CartSection from "@/components/CartSection";
import { FC } from "react";

interface CartProps {}

const Cart: FC<CartProps> = ({}) => {
  return (
    <>
      <CartSection image={""} name={""}  />
    </>
  );
};

export default Cart;
